const styles = theme => ({
  root: {
    width: '100%',
    marginTop: theme.spacing.unit * 3
  },
  table: {
    minWidth: 220
  },
  tableWrapper: {
    overflowX: 'auto'
  }
});

export default styles;
